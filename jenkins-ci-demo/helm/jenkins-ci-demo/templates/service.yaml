apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-ci-demo.fullname" . }}
  labels:
    {{- include "jenkins-ci-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-ci-demo.selectorLabels" . | nindent 4 }}
