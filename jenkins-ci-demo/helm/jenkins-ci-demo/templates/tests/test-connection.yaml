apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-ci-demo.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-ci-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-ci-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
